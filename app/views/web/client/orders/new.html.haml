= simple_form_for @order, url: client_orders_path do |f|
  = f.association :service_price, collection: []
  = f.button :submit, :class => "btn btn-info"
  = link_to t('cancel'), client_orders_path, :class => "btn btn-info"
