= simple_form_for @order, url: admin_order_path(@order.id) do |f|
  = f.association :service_price, collection: []
  = f.button :submit, :class => "btn btn-primary"
  = link_to t('cancel'), admin_orders_path, :class => "btn btn-primary"
