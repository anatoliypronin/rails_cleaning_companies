= simple_form_for @admin, url: admin_admins_path do |f|
  = f.input :name
  = f.input :email
  = f.input :role
  = f.input :password
  = f.button :submit, class: 'btn btn-info'
  = link_to t('cancel'), admin_admins_path, :class => "btn btn-info"
