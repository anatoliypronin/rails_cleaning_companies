= simple_form_for @client, url: admin_client_path(@client.id) do |f|
  = f.input :name
  = f.input :surname
  = f.input :email
  = f.input :phone_number
  = f.input :password_digest
  = f.button :submit
