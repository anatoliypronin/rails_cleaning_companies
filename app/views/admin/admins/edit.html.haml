= simple_form_for @admin, url: admin_admin_path(@admin.id) do |f|
  = f.input :name
  = f.input :email
  = f.input :role
  = f.input :password_digest
  = f.button :submit
  = link_to 'Отмена', admin_admins_path
