%nav.navbar.navbar-dark.bg-primary
  =link_to t('header.home'), root_path, class: 'navbar-brand'
  %ul.navbar-nav
  - unless current_admin.nil?
    =current_admin.email
    =link_to t('header.exit'), admin_session_path, method: :delete, class:'navbar-brand'